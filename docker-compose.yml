version: "3"

services:

  # db:
  #   image: postgres
  #   environment:
  #     - POSTGRES_USER
  #     - POSTGRES_PASSWORD
  #   ports:
  #     - "5432:5432"
  #


  web:
    build: .
    image: flask_app2
    restart: always
    volumes:
      - ./src:/app
    environment:
      - CONSUMER_KEY
      - CONSUMER_SECRET
      - PYTHONIOENCODING
      - FLASK_APP
    ports:
      - 8080:8080
    # depends_on:
    #   - db
